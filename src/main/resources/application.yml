spring:
  datasource:
    url: jdbc:mysql://localhost:3306/community?serverTimezone=UTC&useSSL=false
    username: root
    password: Lee4056!6685
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create   # ?? ??? create / create-drop / update ? ??
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug